{{ form_start(organisation_form) }}
<div class="navbar-form navbar-right">
    <div class="form-group">
        {{ form_widget(organisation_form.groups, { 'attr': {'class': 'form-control' }}) }}
        {#<input type="text" class="form-control" placeholder="{{ 'app.menu.search'|trans }}">#}
    </div>
    {{ form_widget(organisation_form.save, { 'attr': {'class': 'btn btn-default'}}) }}
    {#<button type="submit" class="btn btn-default">{{ 'app.menu.search'|trans }}</button>#}
</div>
{{ form_end(organisation_form) }}