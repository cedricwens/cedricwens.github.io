{#
    {% block fos_user_content %}
        {% render url('fos_user_registration_register') %}
    {% endblock %}
#}
{% extends 'base.html.twig' %}
{% block body %}
    {% form_theme form '@App/Form/fields.html.twig' %}
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    <h3 style="text-align: center">{{ 'app.messages.select_user_role_for_department'|trans({'%firstName%': user.firstName, '%lastName%': user.lastName}) }}</h3>
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
